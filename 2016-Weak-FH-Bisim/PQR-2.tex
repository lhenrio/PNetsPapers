\begin{mathpar}
  \small
  \inferrule
    {\inferrule
        {0 \xrightarrow {\delta}_{C_1} 1}
        {C_1
          \models
            \openrule{
              0 \xrightarrow {\delta}_{C_1} 1,\,
              \{\xrightarrow{\delta(x)}_P\},\,
              v_1=a_1\wedge a_1=\delta(x)}
                      {\ostate{0}\xrightarrow{v_1}\ostate{1}}
        }\\
%      \sm{fresh}v         \\
      \inferrule%*[right={L_1}]
        {
%          \sm{fresh}{a_Q} \\
          \inferrule
              {0 \xrightarrow{l}_{C_2} 0}
              % {\ostate{0}\xrightarrow{l}\ostate{0}}
              {C_2
                \models
                \openrule{
                      0 \xrightarrow l_{C_2} 0,\,
                      \sm{Pred}_{C_2}}
                      {\ostate{0}\xrightarrow{l}\ostate{0}}
              }
        }
        {
          \textrm{Q>>R}\models
              \openrule
                  { 0 \xrightarrow {l}_{C_2} 0,\,
                    \{\xrightarrow{acc(x)}_Q\},\,
                    \ v_2=a_2\wedge a_2=acc(x)
                  }
                  {\ostate{0}\xrightarrow{v_2}\ostate{0}}
        }   
    }
    {
     \textrm{P>>(Q>>R)}
     \models
     \openrule
         { 0 \xrightarrow{\delta}_{C_1} 1, \\ 0 \xrightarrow{l}_{C_2} 0,\\
           \{\xrightarrow{\delta(x)}_P,\,\xrightarrow{acc(x)}_Q\}, \\
            a_3=v_1 \wedge v=a_3
           }
         {\ostate{00} \xrightarrow{v} \ostate{10}}
      }
        
\end{mathpar}

